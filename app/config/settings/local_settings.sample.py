# flake8: noqa
from .base import *

# settings.py
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.mysql",
        "NAME": "db_name",
        "USER": "db_user",
        "PASSWORD": "hKNtaXOK3([DNi6x",
        "HOST": "localhost",
        "PORT": "3306",
    }
}
